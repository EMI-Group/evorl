# @package _global_

defaults:
  - /agent/pbt-paramsac
  - _self_

pop_size: 128

parallel_train: true
parallel_eval: true
workflow_steps_per_iter: 25

warmup_steps: 100

num_iters: 500

checkpoint:
  save_interval_steps: 50
